import {SdkConfig} from '../../sdk-config';

export const mockSdkConfigWithFormServiceConfig: Partial<SdkConfig> = {
    formServiceConfig: {
        apiPath: 'sample_api_path',
        formConfigDirPath: 'sample_dir_path'
    }
};
