import {SdkConfig} from '../../sdk-config';

export const mockSdkConfigWithSystemSettingsConfig: Partial<SdkConfig> = {
    systemSettingsConfig: {
        systemSettingsApiPath: 'SAMPLE_API_PATH',
        systemSettingsDirPath: 'SAMPLE_DIR_PATH'
    }
};
