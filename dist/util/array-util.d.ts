export declare class ArrayUtil {
    static joinPreservingQuotes(array: string[]): string;
    static isEmpty<T>(array: T[]): boolean;
    static contains(array: string[], item: string): boolean;
    static deDupe(array: any): string[];
}
